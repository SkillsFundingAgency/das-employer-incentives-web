@model SFA.DAS.EmployerIncentives.Web.ViewModels.Apply.SelectApprenticeships.SelectApprenticeshipsViewModel

@{
    var previousPage = Model.CurrentPage - 1;
    var nextPage = Model.CurrentPage + 1;
}

@if(Model.PreviousPages || Model.MorePages)
{
    <nav role="navigation" aria-label="Pagination" class="govuk-!-margin-top-9 govuk-!-margin-bottom-6">
        <p class="govuk-body govuk-!-margin-bottom-2">Showing @Model.StartIndex to @Model.EndIndex of all apprentices</p>
        <p class="govuk-body">
            @if (Model.PreviousPages)
            {
                <button type="submit" id="previousPage" name="pageNumber" value="@previousPage" asp-action="SelectApprenticeships" class="das-button--inline-link govuk-!-margin-right-2">Previous <span class="govuk-visually-hidden">page of apprentices</span></button> 
            }
            @if (Model.MorePages)
            {
                <button type="submit" id="nextPage" name="pageNumber" value="@nextPage" asp-action="SelectApprenticeships" class="das-button--inline-link">Next <span class="govuk-visually-hidden">page of apprentices</span></button>
            }
        </p>
    </nav>
}